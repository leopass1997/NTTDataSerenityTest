serenity {
    take.screenshots = FOR_FAILURES
}

##headless.mode = true
webdriver {
    driver = chrome
    autodownload = true
    capabilities {
        "goog:chromeOptions" {
        args = [ "--remote-allow-origins=*"]
        }
    }
}